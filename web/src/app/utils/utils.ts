// import { SanityDocument } from "sanity-codegen";

// export const _linkResolver = (node: SanityDocument) => {
//   if (!node._type) return "/";

//   switch (node._type) {
//     case "project":
//       return `/project/${node.slug.current}`;
//     case "space":
//       return `/space/${node.slug.current}`;
//     default:
//       return `/${node.slug.current}`;
//   }
// };
